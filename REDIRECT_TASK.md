# ЗАДАЧА: свой редирект для ссылок на бота (аналитика по группам)

## Зачем
Ссылка на бота `t.me/LaCiudad_PostBot?start=gID` не проходит антиспам в чужих группах.
Решение: свой редирект на сервере. В объявлении стоит красивая ссылка вида
`https://<домен>/b/1`, которая редиректит на `t.me/LaCiudad_PostBot?start=g1`.
Антиспам видит обычный сайт (не t.me бота) → пропускает.
Метка `?start=g1` долетает до бота → аналитика по группам сохраняется.

## ЧТО УЖЕ РАБОТАЕТ (НЕ ТРОГАТЬ)
- Гостевой оффер в боте: картинка CLUB ACCESS + текст подписки + кнопка «Начать подготовку» → Tribute. Замержено в main.
- Лиды по метке `?start=gID` пишутся автоматически в db['leads']. ПРОВЕРЕНО на живом тесте 7 июня — лид с источником «Испания чат | СНГ» записался корректно.
- `/leads` показывает авто + ручные лиды.
- Кнопка Tribute: https://t.me/tribute/app?startapp=sRi5
- Бот: @LaCiudad_PostBot. Admin ID: 835260826.

## СООТВЕТСТВИЕ gID ↔ ГРУППА (из DEFAULT_GROUPS)
- g1  = Испания чат | СНГ (@spainchats)
- g2  = Испания | Для Своих (@spain_chatss)
- g3  = Валенсия / Costa Blanca (@costablanka)
- g4  = Испания чат | Spain News (@SpainNewsChat)
- g5  = Наши в Мадриде
- g6  = Digital Nomad Spain (@chatfornomads)
- g7  = Марбелья / Costa del Sol (@Marbella_reklama)
- g8  = Business в Испании (@konciergemarket)
- g9  = ВНЖ Испании Чат
- g10 = Русские в Малаге (@russkie_malaga)
- (реальная база на сервере в ~/citypostbot_data.json может отличаться — свериться через /groups)

## ОТВЕТЫ ДРУГА (получены 7 июня) — ВСЁ ГОТОВО К СТАРТУ
1. Домен: **tg.hcs-tomsk.ru** — уже подключён к этому серверу, используем его.
2. Веб-сервер: делаем **ОТДЕЛЬНЫЙ СЕРВИС** (друг сказал — так быстрее запустить).
3. Порт: **8080 свободен** — поднимаем сервис на нём.

→ ПЛАН: лёгкий Python-сервис (http.server / Flask) на порту 8080, маршруты /b/1..N → 302 redirect на t.me/LaCiudad_PostBot?start=gN. systemd-юнит по образцу citypostbot.service. Домен tg.hcs-tomsk.ru проксируется/слушает на 8080 (друг настроит проброс, если нужно). Финальные ссылки в объявлениях: https://tg.hcs-tomsk.ru/b/1 ... /b/15.
→ Друг (поддержка, его сервер) на связи СЕЙЧАС — деплоить, пока доступен.

## ПЛАН РЕАЛИЗАЦИИ (варианты по ответам друга)
- Если есть nginx/caddy → проще всего отдать редиректы прямо в конфиге веб-сервера
  (map gID → t.me ссылка, 302 redirect). Минимум кода, максимально надёжно.
- Если ничего нет → поднять крошечный Python-сервис (Flask/http.server) на свободном порту,
  systemd-юнит по образцу citypostbot.service, проброс через веб-сервер или напрямую.
- Редирект должен поддерживать пути /b/1 ... /b/15 (или /g1 ... /g15) → t.me/LaCiudad_PostBot?start=gN

## ПРАВИЛА ПРОЕКТА (соблюдать всегда)
- Тестировать на feature/ ветке ПЕРЕД мержем в main. main = автодеплой, сломанный main = простой.
- ast.parse ПЕРЕД каждым коммитом.
- НЕ править .py с кириллицей через sed (ломает UTF-8) — только str_replace/Python replace.
- Оксана НЕ имеет прямого доступа к серверу — только через друга (поддержку). Не просить её лазить в логи.
- Доступы: страница «Отдел Автоматизации» в Notion (ID 3774c9eb6e0081caad10c7bd49517297).

## РЕПО
oksiomas-cpu/citypostbot-vercel, главный файл bot_local.py, ветка main.
Деплой бота: GitHub Actions при пуше в main (SSH на сервер + рестарт citypostbot.service).
Редирект-сервис — отдельный, деплой/перезапуск через друга.
